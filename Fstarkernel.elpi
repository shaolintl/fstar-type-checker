% kernel (taken from PTS_kernel_machine which works with main_cic query in test_kernel_cic)
accumulate PTS_cic.

% added the two below. lets see what happens and see notes
accumulate PTS_typing_refiner_machine.
accumulate PTS_machine_refiner.


accumulate PTS_typing.
accumulate PTS_abbr_typing.
accumulate PTS_machine.

accumulate PTS_inductives.
accumulate PTS_global.
accumulate Fstartest.


